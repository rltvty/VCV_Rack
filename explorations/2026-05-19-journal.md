## 05:16 CEST - Tightened 8/16 channel horizontal spacing

- Current goal: fit more channels per HP in the split audio modules without changing the 2-channel device.
- Files changed:
  `standalone/RltvtyAudioIO/src/AudioSplit.cpp`
- Changes made:
  reduced `Audio * 8` width from `19 HP` to `17 HP`
  reduced `Audio * 16` width from `38 HP` to `34 HP`
  trimmed side margins for the one-row jack layout:
  8-channel modules now use `7.0 mm` side margins
  16-channel modules now use `6.5 mm` side margins
- Unchanged:
  `Audio * 2` remains `5 HP`
  the overall one-row layout, per-channel metering, and flipped `Audio Out` orientation remain intact
- Build/install status:
  rebuilt successfully with `make install`
- Next step:
  restart Rack and verify the tighter spacing still feels patchable and that the ports are not visually too cramped for real cable use

## 05:18 CEST - Mirrored `Audio Out` jack spacing against `Audio In`

- User requested that the `Audio Out` ports mirror the `Audio In` spacing: the distance from the top rack lane to the output jacks should match the distance from the input jacks to the bottom rack lane.
- Change made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  moved the `Audio Out` jack row from `42 mm` to `23 mm`
  moved the matching VU meter stack down anchor from `63 mm` to `48 mm`
- Unchanged:
  `Audio In` layout was left alone
  `Audio Out` still keeps the same vertical ordering:
  jacks on top
  meters below
  device controls at the bottom
- Build/install status:
  rebuilt successfully with `make install`

## 05:26 CEST - Added per-channel number labels

- User requested visible channel numbering near the jacks.
- Change made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  added a lightweight `ChannelNumberLabel` widget
  each channel now renders its number in the gap between the jack and the VU meter stack
  because the `Audio Out` layout is inverted, the same placement rule naturally puts the labels below the jacks on outputs and above the jacks on inputs
- Build/install status:
  rebuilt successfully with `make install`

## 05:29 CEST - Corrected input-side label placement and strengthened typography

- User reported that the channel numbers were visible on `Audio Out` but not on `Audio In`, and requested larger / bolder labels centered more clearly between each jack and meter stack.
- Root cause:
  the first implementation centered labels on `Audio In` against the top of the VU stack, which placed them too close to the meter area instead of in the lower gap near the jacks
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  `Audio In` labels now center between the jack row and the bottom of the VU meter stack
  `Audio Out` labels still center between the jack row and the top of the VU meter stack
  switched label font to `Nunito-Bold`
  increased label size and darkened / brightened the text color for better contrast
- Build/install status:
  rebuilt successfully with `make install`

## 05:31 CEST - Final label centering nudge

- User requested one more small centering pass:
  `Audio In` labels slightly higher
  `Audio Out` labels slightly lower
- Change made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  applied a `-1.0 mm` vertical nudge to input-side labels
  applied a `+1.2 mm` vertical nudge to output-side labels
- Build/install status:
  rebuilt successfully with `make install`

## 05:36 CEST - Made channel labels follow device offsets and live channel count

- User requested that channel numbers reflect the selected device offset, for example showing `9..16` when an 8-channel module is mapped to channels `9-16` on a larger device.
- Additional requirement:
  if the attached device has fewer active channels than the module width, unsupported positions should not pretend to be valid channels
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  channel labels are now updated dynamically during widget `step()`
  label numbering uses `inputOffset` or `outputOffset` from the selected device mapping
  when a live device is attached, labels beyond the active supported channel count now show `N/A`
  when the device is missing but a locked mapping exists, labels still show the configured offset-based channel numbers
- Practical result:
  selecting a range like `9-16` should render `9 10 11 12 13 14 15 16`
  a smaller live device mapped into a wider module should show valid channels first and `N/A` for unsupported positions
- Build/install status:
  rebuilt successfully with `make install`

## 06:00 CEST - First gain-control slice on 8-channel modules

- Started the agreed first implementation slice for gain handling, limited to the 8-channel split modules on both device types.
- Files changed:
  `standalone/RltvtyAudioIO/src/AudioSplit.cpp`
- DSP/parameter changes:
  added a `master gain` parameter
  added per-channel gain parameters
  gain range is `-12 dB` to `+12 dB`
  gain controls use `1 dB` snap steps
  total channel gain is `master dB + channel dB`
  added a `meter tap` switch with `pre-gain` / `post-gain`
  both `Audio In` and `Audio Out` signal paths now apply gain before sending audio onward
  VU meters now read either pre-gain or post-gain depending on the switch
- UI changes for 8-channel modules:
  added a slim left-side master strip
  master strip currently contains:
  master gain trimpot
  meter pre/post switch
  added staggered per-channel gain trimpots in the empty space around the meter stacks
  left the existing patch rows, labels, and meter columns in place
- Scope intentionally deferred:
  no clip latching yet
  no clip reset button yet
  no auto-reset timing switch yet
  no 2-channel or 16-channel gain UI yet
- Build/install status:
  rebuilt successfully with `make install`
- Next step:
  restart Rack and visually test whether the 8-channel gain controls fit and feel usable before layering on clip behavior

## 06:12 CEST - Widened 8-channel modules for a proper master strip

- User reviewed the first gain UI and preferred a less cramped layout:
  restore the previous 8-channel spacing
  allocate explicit width on the left for the master section instead of squeezing it into the existing channel block
  use larger knobs, especially for master gain
  add a visible separator between master and channel sections
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  widened 8-channel modules from `27 HP` to use an explicit `8 HP` master strip plus the prior `19 HP` channel area
  restored the earlier wider channel spacing inside the 8-channel section
  upgraded master gain to `RoundSmallBlackKnob`
  upgraded per-channel gains to `BefacoTinyKnob`
  added a triple-line vertical divider in the panel drawing to separate the master strip from the channel area
  repositioned master labels and controls to take advantage of the wider strip
- Build/install status:
  rebuilt successfully with `make install`

## 07:59 CEST - Tightened the master strip and limited the divider span

- User wanted the 8-channel master section to be a bit leaner while still making the master gain knob more prominent.
- Additional UI request:
  keep the section divider only inside the white control field instead of running it behind the black device configuration displays.
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  reduced the 8-channel module width from `27 HP` to `25 HP`
  shrank the dedicated master strip from `8 HP` to `6 HP`
  kept the restored `19 HP` channel area intact
  upgraded the master gain control again to `RoundLargeBlackKnob`
  changed the custom divider drawing so it only spans the open control area and stops short of the top/bottom device display regions
  kept the per-channel gain controls and main channel spacing unchanged
- Design effect:
  master controls should read more intentionally as a compact left utility strip
  the channel block should stay visually familiar
  the divider should no longer clutter the configuration display area
- Build/install status:
  rebuilt successfully with `make install`

## 08:03 CEST - Cleaned up master-strip labels

- User liked the revised 8-channel master section but wanted the legend text to read more clearly.
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  renamed `MASTER` to `MAIN`
  moved `METER` to the left side of the meter-tap switch so it reads as a side label
  moved `PRE` above the switch while keeping `POST` below it
- Design effect:
  the left control strip should read less cramped and the meter-tap control should look more intentional
- Build/install status:
  rebuilt successfully with `make install`

## 08:04 CEST - Flattened channel gain knobs into one row

- User review of the latest panel screenshot suggested two follow-up tweaks:
  move `METER` a little farther left
  stop staggering the per-channel gain knobs now that the 8-channel spacing is wider again
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  nudged the `METER` label farther left from the switch
  replaced the alternating per-channel gain knob Y positions with one shared row on `Audio In 8` and one shared row on `Audio Out 8`
- Design effect:
  the master-strip legend should read more cleanly
  the channel gain section should look more orderly and less busy if the one-row fit is comfortable
- Build/install status:
  rebuilt successfully with `make install`

## 21:45 CEST - Fixed PRE/POST meter switch polarity

- User finally validated live audio and gain behavior and found the first actual logic bug in the new gain feature set:
  audio routing and both master/per-channel gains worked
  the `PRE/POST` meter switch was reversed, with the `PRE` position showing post-gain metering
- Cause:
  the physical switch orientation on the panel did not match the original `METER_TAP_PARAM` interpretation in code
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  inverted `isMeterPostGain()` so the top `PRE` position maps to pre-gain metering
  swapped the configured switch option labels to match the corrected value semantics
- Build/install status:
  rebuilt successfully with `make install`

## 21:53 CEST - End-to-end gain and meter validation

- User tested a more intentionally odd patch routing to validate the new control path instead of only checking a straight-through case.
- Confirmed working together:
  audio routing through `RltvtyAudioIO`
  per-channel gain behavior
  master gain behavior
  VU metering response
  corrected `PRE/POST` meter tap switch behavior
- Practical result:
  the current 8-channel gain feature slice appears functionally sound, not just visually in place
- Likely next step:
  continue with clip indicator design or broaden the gain-control feature set to the 2-channel / 16-channel variants

## 21:56 CEST - Added first latched clip slice to the 8-channel modules

- Started the next agreed feature pass on `Audio In 8` and `Audio Out 8` only:
  per-channel clip indicators
  latched clip state
  master clip reset control
  3-way auto-reset timing switch
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  added per-channel clip light state and timers inside `SplitAudioModule`
  added a momentary `CLIP RST` button in the master strip
  added a 3-position clip auto-reset switch with `3s`, `10s`, and `OFF/manual`
  implemented clip detection from the same pre/post tap point already used by the VU meters
  implemented manual clear plus optional timed auto-clear per channel
  added separate per-channel red clip LEDs in the channel strip, distinct from the VU stack
- Design note:
  clip detection currently follows the selected `PRE/POST` meter point, which matches the earlier gain-path design choice rather than introducing a separate clip-tap mode
- Scope:
  this is still only the 8-channel UI slice
  2-channel and 16-channel modules do not yet expose the new clip controls
- Build/install status:
  rebuilt successfully with `make install`

## 22:11 CEST - Corrected clip-mode legend ordering

- User found that the `3S` and `OFF` labels around the new 3-way clip auto-reset switch were reversed relative to the actual switch positions.
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  swapped the top and bottom clip-mode legend labels so the printed order matches the switch
  increased the label size for all three mode options to improve readability
- Build/install status:
  rebuilt successfully with `make install`

## 22:12 CEST - Moved clip LEDs above the VU stacks

- User wanted the per-channel clip indicators to sit above the VU meters on both `Audio In 8` and `Audio Out 8`, with a clearer visual gap so they do not read like an extra VU segment.
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  changed the clip LED Y position to a fixed point above the top VU segment instead of deriving it from the channel-label region
  added a small explicit vertical gap between the clip LED and the VU stack
- Build/install status:
  rebuilt successfully with `make install`

## 22:19 CEST - Redesigned the 8-channel VU stacks

- User wanted more vertical resolution and a more mixer-like color treatment for the 8-channel meters:
  a larger gap between clip LEDs and the stack
  more VU segments
  green/blue/red color bands
  slightly larger individual meter LEDs
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  increased the 8-channel VU stacks from `4` segments to `8` segments
  added a dedicated `VuSimpleLight` widget sized slightly larger than the prior simple lights
  changed the 8-channel VU color bands to:
  bottom green
  middle blue
  top red
  tightened VU segment spacing to fit the denser stack in the same general panel region
  increased the gap between the clip LED and the top of the VU stack
  left the non-8-channel meter layouts unchanged
- Build/install status:
  rebuilt successfully with `make install`

## 22:23 CEST - Retuned the 8-channel VU ladders toward Xone-style thresholds

- User wanted the denser 8-channel VU stacks to use more of the available vertical space while keeping the new clip gap intact.
- Additional color/threshold request:
  only the final top segment should be red
  the first blue segment should begin at `0 dB`
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  increased 8-channel meter spacing to make the ladders taller
  pushed the clip LED a little farther above the top of the stack
  changed the 8-channel color split to:
  one red segment at the top
  two blue segments beneath it
  remaining lower segments green
  remapped the 8-channel meter thresholds so blue begins at `0 dB` and red appears only at the final highest segment
- Build/install status:
  rebuilt successfully with `make install`

## 22:27 CEST - Aligned input gain row and raised output jack row

- User wanted one more structural layout pass after the taller 8-channel VU redesign:
  move the `Audio In 8` channel gain row upward so it lines up with the `MAIN` gain knob center
  move the `Audio Out 8` jack row a bit higher
- Changes made in `standalone/RltvtyAudioIO/src/AudioSplit.cpp`:
  changed the 8-channel input gain knob row from `64.0 mm` to `56.0 mm`, matching the input-side master gain center
  moved the 8-channel output port row from `23.0 mm` to `20.0 mm`
- Build/install status:
  rebuilt successfully with `make install`
