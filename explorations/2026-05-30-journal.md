## 21:10 CEST - Extended the enhanced audio-module UI/features to 2 and 16 channels

- Revisited the split audio plugin after a week of real-world use on the 8-channel modules.
- Goal for this pass:
  bring the `2` and `16` channel variants closer to the `8` channel experience instead of leaving them on the older reduced UI/feature set.
- Implemented in [standalone/RltvtyAudioIO/src/AudioSplit.cpp](/Users/epinzur/src/github.com/rltvty/VCV_Rack/standalone/RltvtyAudioIO/src/AudioSplit.cpp:1):
  all module sizes now expose the left-side master strip
  all module sizes now have `ALT` failover ports and the failover mode switch
  all module sizes now use the larger multi-segment VU meters and per-channel clip indicators
  all module sizes now include per-channel gain plus the master controls rather than limiting those features to `8`
- Practical implementation choice:
  the underlying DSP/behavior model is now shared across the module sizes, while layout still adapts by width and channel count
- Open questions for testing:
  whether the `2` channel layout feels balanced with the new master strip width
  whether the `16` channel spacing is still comfortable enough for patching and reading meters
  whether any of the generalized row positions need size-specific tuning after real use

## 21:18 CEST - Added dedicated 20-in and 32-out module variants

- Added two new concrete models to the split audio plugin:
  `Audio In 20`
  `Audio Out 32`
- Implementation approach:
  reused the existing templated module/widget system instead of creating one-off code paths
  registered the models in [standalone/RltvtyAudioIO/src/plugin.cpp](/Users/epinzur/src/github.com/rltvty/VCV_Rack/standalone/RltvtyAudioIO/src/plugin.cpp:1)
  exposed them in [standalone/RltvtyAudioIO/plugin.json](/Users/epinzur/src/github.com/rltvty/VCV_Rack/standalone/RltvtyAudioIO/plugin.json:1)
- Important practical choice:
  used explicit wider panel sizes for `20` and `32` channels rather than trying to compress them into the existing `16` channel width
- Reason for this scope:
  the user does not need the full `64` BlackHole path yet, so `32-out` is a lower-risk test of the large-module ergonomics while still being immediately useful
